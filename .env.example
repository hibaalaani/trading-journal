# Optional: only needed if frontend is hosted separately from the API
# VITE_API_URL=https://your-api.railway.app/api

# Optional: for PostgreSQL (replace SQLite) – see prisma/schema.prisma
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"
